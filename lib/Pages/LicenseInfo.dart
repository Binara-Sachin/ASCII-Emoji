import 'package:flutter/material.dart';

class LicenceInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Licence Info"),
      ),
      body: Center(
        child: Text('Licence Info Here'),
        ),
    );
  }
}